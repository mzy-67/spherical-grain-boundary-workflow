# Single-GB example

`orientation.csv` contains the first GB entry from the 1,291-structure table. Use `--start-row 0 --stop-row 1 --dry-run` to validate repository and cluster configuration before submitting a production job.
