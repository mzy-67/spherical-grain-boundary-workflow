# Legacy source snapshot

`gbmaker2_original.py` is retained only to document pre-publication provenance. Do not import or run it for new calculations. The maintained implementation is `src/jbfuncs/gbmaker2.py`, which preserves the reported scientific defaults while adding configuration, validation, and explicit LAMMPS failure handling.
